# CLAUDE.md — 项目上下文（请先读这份）

## 这是什么

一个**移动端现场调研工具**：售前/BD 去 3PL 仓储项目现场时，手机边走边填、拍照记录，当场导出图文调研报告。

## 最重要：定位边界（不要越界）

本工具**只负责采集与出报告**。下面这条线不能模糊：

```
现场调研采集器（本工具）──出图文报告──► 人看 + 喂给 Logistics Presale System 做测算
```

### ✅ 要做
- 结构化字段采集（仓储货物、用工现状、瓶颈、空间结构、现场观察等客观事实）
- 拍照记录 + AI 图注（核心能力，重点打磨）
- 一键导出图文报告（Markdown 单文件 + 图文 ZIP）
- 本地自动保存（localStorage），断网可用

### ❌ 绝对不要做（这些归 Logistics Presale System，不在本工具范围）
- **任何计算**：人数、用工成本、毛利、报价、投资回收期、ROI
- 与 Presale System 的接口对接（用户复用其 NotebookLM 导入能力，本工具只输出 NotebookLM 友好的报告即可）
- 云端账户、多人协作、服务端

> 商务/预算类字段（计费方式、预算、报价时间等）**只作纯文本记录**，绝不加任何运算逻辑。若发现自己在写计算函数，停下——那是另一个系统的职责。

## 技术约束

- **单文件 HTML**：一个 `.html` 文件搞定，HTML + CSS + 原生 JS 内联，方便手机直接打开、加主屏、离线用。
- **无构建步骤**：不引入打包工具、不依赖 npm 运行时。字体等可用 CDN，但断网时要能降级。
- **移动端优先**：手机浏览器为主要场景，触控友好。
- **数据只存本地**：`localStorage` 自动保存；任何数据不上传服务器。
- **AI 图注**：调用用户自己填入的 API（key 仅存本机浏览器，不上传）；无网络时手填图注照常工作，不能阻塞调研。
- **UI 中文**。

## 现有基础

`tools/汽配仓储调研.html` 是 v1.0 的起点原型（来自汽配仓储项目实战）。
> ⚠️ 若仓库 tools/ 下还没有这个文件，请先把它放进去；v1.0 是在它基础上改造，不是从零重写。

原型已有：7 大调研板块、移动端、拍照 + AI 图注、localStorage 自动保存、Markdown + ZIP 导出。

## v1.0 要做的事

详见 `docs/v1.0-spec.md`。概括为四点：
1. 强化拍照 / 图注体验（按板块归类、现场标注、图文排版）
2. 测算类字段降级为纯记录（移除任何计算逻辑）
3. 报告可读性 + NotebookLM 友好（结构清晰、信息密度高）
4. 字段通用化收口（适配汽配/工程机械/电商/医药/快消等不同仓）

## 如何验证

- 用手机浏览器（或桌面浏览器移动模拟）打开 HTML
- 走一遍：填字段 → 拍照/选图 → 写图注 → 导出 Markdown / ZIP
- 关网络再试一遍，确认离线可用、AI 图注优雅降级

## 约定

- 改动尽量内聚在单文件内；保持现有视觉风格的一致性
- 数据结构（SCHEMA）改动要兼容已存的 localStorage（做好版本与迁移，别让用户旧数据丢失）
- 提交信息用中文，简明说明改了什么
