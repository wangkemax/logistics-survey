services:
  survey-tool:
    image: nginx:alpine
    container_name: presale-survey
    restart: unless-stopped
    ports:
      - "8327:80"
    volumes:
      - ./tools:/usr/share/nginx/html:ro